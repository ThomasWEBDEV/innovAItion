    /** @type {import('next').NextConfig} */
    const nextConfig = {
      // Ajoutez vos configurations Next.js ici si nécessaire
      // Pour l'intégration de PostCSS/Tailwind, Next.js le gère généralement automatiquement
      // mais cette configuration est un point de départ si des problèmes surviennent.
    };

    module.exports = nextConfig;
